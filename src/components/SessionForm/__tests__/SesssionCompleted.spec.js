import React from 'react'
import {expect} from 'chai'
import {shallow} from 'enzyme'
import {SessionCompleted} from '../SessionCompleted'

describe('<SessionCompleted/>', () => {

  it('renders', () => {
    const wrapper = shallow(<SessionCompleted/>)
  })
})
